/* *
* Despite its name, the auth module is only responsible for retrieving the UUID from /api/auth 
* that was created on submitting a search in the explore view/SearchController.
* */

(function() {
  'use strict';

  angular.module('app.auth', []);
})();
